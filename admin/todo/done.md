- [x] Create data class for projects to pass into add command.
- [x] Create data class for activities to pass into add command.
- [x] Create data class for tasks to pass into add command.
- [x] Create data class for categoried to pass into add command.
- [x] Create data class for subcategories to pass into add command.
- [x] Create data class for employees to pass into add command.
- [x] Create data class for fields to pass into add command.
- [x] Create data class for milestone steps to pass into add command.
- [x] Implement the listNotes() function in ./src/commands/list.cpp
- [x] Implement the listArtefactTypes() function in ./src/commands/list.cpp
- [x] Implement the listArtefacts() function in ./src/commands/list.cpp
- [x] Implement the listFields() function in ./src/commands/list.cpp
- [x] Implement the listMilestones() function in ./src/commands/list.cpp
- [x] Implement the listMilestoneSteps() function in ./src/commands/list.cpp
- [x] Implement the selectNote() function in ./src/commands/select.cpp
- [x] Implement the selectArtefactType () function in ./src/commands/select.cpp
- [x] Implement the selectArtefact () function in ./src/commands/select.cpp
- [x] Implement the selectField () function in ./src/commands/select.cpp
- [x] Implement the selectMilestone () function in ./src/commands/select.cpp
- [x] Implement the selectMilestoneStep () function in ./src/commands/select.cpp
- [x] Implement the removeNote() function in ./src/commands/remove.cpp
- [x] Implement the removeArtefactType() function in ./src/commands/remove.cpp
- [x] Implement the removeArtefact() function in ./src/commands/remove.cpp
- [x] Implement the removeField() function in ./src/commands/remove.cpp
- [x] Implement the removeMilestone() function in ./src/commands/remove.cpp
- [x] Implement the removeMilestoneStep() function in ./src/commands/remove.cpp
- [x] Implement the addArtefactType() function in ./src/commands/add.cpp
- [x] Implement the addArtefact() function in ./src/commands/add.cpp
- [x] Implement the addNote() function in ./src/commands/add.cpp
- [x] Implement the addField() function in ./src/commands/add.cpp
- [x] Implement the addMilestone() function in ./src/commands/add.cpp
- [x] Implement the addMilestoneStep() function in ./src/commands/add.cpp
- [x] Implement the ProjectData(std::string csvData) constructor in ./src/data/projectdata.cpp to build the records vector from a string containing csv data (with the first row having the field titles: projectname, projectno, projectmanagerid). 
- [x] Implement the validate() function in ./src/data/projectdata.cpp
- [x] Update ./src/data/employeedata.h and .src/data/employeedata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/activitydata.h and .src/data/activitydata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/artefactdata.h and .src/data/artefactdata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/artefacttypedata.h and .src/data/artefacttypedata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/categorydata.h and .src/data/categorydata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/fielddata.h and .src/data/fielddata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/milestonedata.h and .src/data/milestonedata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/milestonestepdata.h and .src/data/milestonestepdata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/notedata.h and .src/data/notedata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/projectdata.h and .src/data/projectdata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/subcategorydata.h and .src/data/subcategorydata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data.
- [x] Update ./src/data/taskdata.h and .src/data/taskdata.cpp in line with projectdata to handle multiple data, implementing a next(), reset() and validate() functions, and a constructor that builds the records vector from a string containing csv data. 
- [x] Update function addProject() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addCategory() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addSubcategory() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addActivity() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addTask() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addArtefactType() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addArtefact() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addNote() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addField() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addMilestone() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Update function addMilestoneStep() in ./src/commands/add.cpp in line with addEmployee() to handle multiple data, check for duplicates and diplay appropriate error messages.
- [x] Create a sample csv data for employees that can be imported into ./src/data/employeedata.h. Save the file as ../database/sample_csv/employeedata.csv
- [x] Create a sample csv data for activities that can be imported into ./src/data/activitydata.h. Save the file as ../database/sample_csv/activitydata.csv
- [x] Create a sample csv data for artefacts that can be imported into ./src/data/artefactdata.h. Save the file as ../database/sample_csv/artefactdata.csv
- [x] Create a sample csv data for artefacttypes that can be imported into ./src/data/artefacttypedata.h. Save the file as ../database/sample_csv/artefacttypedata.csv
- [x] Create a sample csv data for categories that can be imported into ./src/data/categorydata.h. Save the file as ../database/sample_csv/categorydata.csv
- [x] Create a sample csv data for fields that can be imported into ./src/data/fielddata.h. Save the file as ../database/sample_csv/fielddata.csv
- [x] Create a sample csv data for milestones that can be imported into ./src/data/milestonedata.h. Save the file as ../database/sample_csv/milestonedata.csv
- [x] Create a sample csv data for milestonesteps that can be imported into ./src/data/milestonestepdata.h. Save the file as ../database/sample_csv/milestonestepdata.csv
- [x] Create a sample csv data for notes that can be imported into ./src/data/notedata.h. Save the file as ../database/sample_csv/notedata.csv
- [x] Create a sample csv data for projects that can be imported into ./src/data/projectdata.h. Save the file as ../database/sample_csv/projectdata.csv
- [x] Create a sample csv data for subcategories that can be imported into ./src/data/subcategorydata.h. Save the file as ../database/sample_csv/subcategorydata.csv
- [x] Create a sample csv data for tasks that can be imported into ./src/data/taskdata.h. Save the file as ../database/sample_csv/taskdata.csv
- [x] Create a constructor taking arguments (Database& db, int projectId=0) in .src/data/projectdata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int activityId=0) in .src/data/activitydata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int artefactId=0) in .src/data/artefactdata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int artefactTypeId=0) in .src/data/artefacttypedata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int categoryId=0) in .src/data/categorydata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int fieldId=0) in .src/data/fielddata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int milestoneId=0) in .src/data/milestonedata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int milestoneStepId=0) in .src/data/milestonestepdata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int noteId=0) in .src/data/notedata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int subcategoryId=0) in .src/data/subcategorydata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a constructor taking arguments (Database& db, int taskId=0) in .src/data/taskdata.h similar to the constructor EmployeeData(Database& db, int employeeId=0) in the file ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/projectdata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/activitydata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/artefactdata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/artefacttypedata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/categorydata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/fielddata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/milestonedata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/milestonestepdata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/notedata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/subcategorydata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Create a function setValue() in ./src/data/taskdata.h in line with the setValue() function in ./src/data/employeedata.h
- [x] Complete the set command implementation in ./src/commands/set.h to set a field value.
- [x] Add an export command to export data to a csv file. Create a header file ./src/commands/exportcsv.h for the implementation.
- [x] Add an source command to run multiple commands read from an source file. Create a header file ./src/commands/source.h for the implementation.
- [x] Modify setProjectData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setActivityData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setCategoryData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setSubcategoryData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setTaskData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setArtefactTypeData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setArtefactData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setNoteData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setFieldData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setMilestoneData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Modify setMilestoneStepData() in line with setEmployeeData() in ./src/commands/set.cpp. Also make the required changes in set() and in set.h file. 
- [x] Issue: Segmentation fault core dump error when trying to create a category.
- [x] Issue: Correct the message when an item is added/imported from "Successfully imported 1 entery" to "Successfully added 1 entry" **Proposed solution:** In `src/commands/add.cpp`, fix the typos in all add functions. Change "Successfully imported" to "Successfully added", "entery" to "entry", "enteries" to "entries", and "No enteries found" to "No entries found". The typo appears in multiple functions: `addProject()` (line 106), `addCategory()` (line 163), `addSubcategory()` (line 220), `addActivity()` (line 281), `addTask()` (line 366), `addEmployee()` (line 416), `addArtefactType()` (line 474), `addArtefact()` (line 532), `addNote()` (line 593), `addField()` (line 675), `addMilestone()` (line 732), and `addMilestoneStep()` (line 790). Use find-and-replace to change all occurrences.
- [x] Issue: New project seems to be refering to the wrong set of activities.  **Proposed solution:** In `src/commands/select.cpp:36`, when navigating to the Activities folder, the code passes the selection number (1) instead of the project ID. Change `pth.move_forward("Activities",id,Path::ACTIVITIES_FOLDER);` to `pth.move_forward("Activities",pth.current_id(),Path::ACTIVITIES_FOLDER);` to preserve the project ID. This ensures that when listing categories or adding new categories, the correct project ID is used.
- [x] Issue: Artefact types, Milestones and Linkages folders seem to be empty.  **Proposed solution:**  In `src/commands/select.cpp`, lines 39, 42, and 45, when navigating to "Artefact Types", "Milestones", and "Linkages" folders, the selection number is passed instead of the project ID. Change all three lines to use `pth.current_id()` instead of `id`: 
- [x] Issue: When creating a category manually the program should not ask for the project id and take it from the Path instead.  **Proposed solution:** In `src/commands/add.cpp:19-21`, the code creates `CategoryData cat;` using the default constructor, which prompts for project ID. Change this to use the constructor that takes a project ID: `CategoryData cat(pth.current_id());`. This constructor (defined in `src/data/categorydata.cpp:19-28`) only prompts for the category name and uses the provided project ID. Note: This fix depends on issue 109/110 being fixed first, so that `pth.current_id()` returns the correct project ID when in ACTIVITIES_FOLDER.
- [x] Issue: All activities are being displayed under a single sub-category instead of separate categories as in the database.  **Proposed solution:** In `src/commands/list.cpp:74-94`, the `listActivities()` function has two bugs: 1. Line 86 uses undefined variable `projectId` when it should use `subCategoryId` 2. Line 87 has wrong WHERE clause: `WHERE ac.subcategoryid = ?` should be `WHERE a.subcategoryid = ?` (using `a.` alias for Activities table, not `ac.` for ActivityCategories)
- [x] Issue: The list command within an activity displays both the folders and an empty task table. Remove the task table.  **Proposed solution:** In `src/commands/list.cpp:38-44`, there is a missing `break` statement after the ACTIVITY case, causing fall-through to the TASKS_FOLDER case. Add `break;` after line 41.
- [x] Issue: Unable to use add within the task folder.  **Proposed solution:** In `src/commands/add.cpp:30`, there is a bug where `pth.current_id()` is compared with `Path::TASKS_FOLDER` enum value instead of using `pth.current_type()`. Change line 30 from:
- [x] Issue: Make the listing of folders and tables consistent when using the list command in ./src/commands/list.h **Proposed solution:** Review all cases in `list()` function in `src/commands/list.cpp` and ensure consistent formatting. Currently, ROOT and PROJECT cases display folder options with numbered lists, while data-containing folders call listing functions that display formatted tables. Consider standardizing the output format - all locations should display the same style of headers.
- [x] Issue: Artefacts and fields seem to be listed under the wrong Artefact type and are duplicated in different projects. **Proposed solution:** In `src/commands/select.cpp` lines 87 and 90, change `id` to `pth.current_id()` when navigating to ARTEFACTS_FOLDER and FIELDS_FOLDER from ARTEFACT_TYPE.
- [x] Represent the ROOT folder using the `~` symbol. 
- [x] Using `cd ~` should get the user back to the root.
- [x] List command should take a `-a` flag that lists the details of the entire table. Without the flag it should list just the IDs and NAMEs in the format : (ID) NAME.  
- [x] The user should be able to navigate the database with the select command using the either the ID or NAME. If the first character is a number search for suitable IDs otherwise search for NAMEs
- [x] Issue: When using the select command the program does not handle NAMEs with spaces in them. **Proposed solution:** The issue is in `src/main.cpp:47-57` where input is tokenized by splitting on spaces, which breaks names containing spaces. When a user types `sl "My Project"`, it gets split into ["sl", "\"My", "Project\""], and only "\"My" is passed as the parameter. Fix by modifying the token parsing logic in `main.cpp` to handle quoted strings.
- [x] When using the select command, pressing tab should auto fill partially filled ID/NAME. 
- [x] Add command should have the ability accept a comma separated string of values and create the required item by parsing the string.
- [x] The interactive source command is not required. Instead call source when the program is invoked with the file path and name as argument. Create a sample source file in ./examples to test this functionality.
- [x] Issue: When trying to import csv the program goes into manual add mode for Artefact types, artefacts, fields, milestones and milestone steps.
- [x] Issue: When importing notes the activity id should be obtained from the current path and not from the csv file.
- [x] Issue: When adding a new item, and the foreign key for a table is invalid, give a more descriptive error message than "Error creating note: Cannot add or update a child row: a foreign key constraint fails (`trial`.`ActivityNotes`, CONSTRAINT `ActivityNotes_ibfk_2` FOREIGN KEY (`userid`) REFERENCES `Employees` (`employeeid`))" **Proposed solution:** In `src/commands/add.cpp:696-698`, the `sql::SQLException` is caught but only the raw MySQL error is displayed. The `addNote` function already validates that the activity ID exists (line 647-651) before inserting, but does not validate the `userid`. Fix by adding a pre-insert check: query `SELECT employeeid FROM Employees WHERE employeeid = ?` with the user-provided userid before attempting the insert. If no match, print a descriptive message like "Error: Employee with ID X not found." Apply this same pattern to all add functions that reference foreign keys to the Employees table.
- [x] Issue: When adding an activity we get the database error: "Error creating activity: Cannot add or update a child row: a foreign key constraint fails (`trial`.`Activities`, CONSTRAINT `Activities_ibfk_2` FOREIGN KEY (`activitymanagerid`) REFERENCES `Employees` (`employeeid`))" **Proposed solution:** In `src/commands/add.cpp:330-388`, the `addActivity` function validates the subcategory ID but does not validate `activitymanagerid` before inserting. Fix by adding a check after the subcategory validation: query `SELECT employeeid FROM Employees WHERE employeeid = ?` with the manager ID from `act.getManagerId()`. If no match, print "Error: Employee with ID X not found. Cannot assign as activity manager."
- [x] Issue: When adding a sub task we get the following error: "Activity with ID 46 not found." **Proposed solution:** In `src/commands/add.cpp:83-101`, when the current path type is `TASK`, the call `addTask(db, tsk, pth.current_id(), 0)` incorrectly passes `pth.current_id()` (which is the parent task ID) as the `activityId` parameter, and `0` as the `parentTaskId`. The activity ID was correctly queried into `actId` on line 89 but not used in the `addTask` call. Fix by changing lines 92 and 97 from `addTask(db, tsk, pth.current_id(), 0)` to `addTask(db, tsk, actId, pth.current_id())`.
- [x] Issue: While importing a task activity id and parent id should not be required this is be identified from the current path. **Proposed solution:** In `src/data/taskdata.cpp:35-88`, the CSV constructor `TaskData(csvData, activityId, parentTaskId)` reads `activityId` and `parentTaskId` from CSV columns 1 and 2, ignoring the parameters passed from the caller. In `src/commands/importcsv.cpp:54-56`, the caller correctly derives these values from the path (`pth.current_id()` for activityId, `0` for top-level tasks). Fix by modifying the CSV constructor to only parse the task name (column 0) from the CSV and use the `activityId` and `parentTaskId` parameters for all records: set `temp.activityId = activityId` and `temp.parentTaskId = parentTaskId` instead of reading them from the CSV. Update the field count check from `< 3` to `< 1`. The CSV file should then only require a header row and task names.
- [x] Issue: Removing an item by name does not seem to work. **Proposed solution:** In `src/commands/remove.cpp:8`, the function guard `if (!param.empty() && utils::isNumber(param))` rejects any non-numeric input, so name-based removal is never attempted. Fix by adding an `else` branch: when `param` is not a number, perform a name-to-ID lookup query based on the current path type (e.g., at `EMPLOYEES_FOLDER` query `SELECT employeeid FROM Employees WHERE CONCAT(firstname,' ',lastname) = ?`, at `ACTIVITIES_FOLDER` query `SELECT categoryid FROM ActivityCategories WHERE categoryname = ? AND projectid = ?`, etc.). If exactly one match is found, use that ID and proceed with the existing removal logic. If no match or multiple matches are found, display an appropriate error message.
- [x] Issue: Removing by id is causing the following error: "Error deleting category: Unknown column 'h.taskid' in 'ON'." **Proposed solution:** In `src/commands/remove.cpp:292-300`, the DELETE query for Hours joins directly on `h.taskid` which does not exist. Per the schema (`database/schema/schema.sql:82-89`), the `Hours` table references `ActivityTaskAssignments` via `assignmentid`, not `ActivityTasks` directly. Fix all Hours DELETE statements (in `removeCategory`, `removeProject`, `removeSubCategory`, `removeActivity`, `removeTask`) to join through `ActivityTaskAssignments`: change `"DELETE h FROM Hours h INNER JOIN ActivityTasks t ON h.taskid = t.taskid ..."` to `"DELETE h FROM Hours h INNER JOIN ActivityTaskAssignments ata ON h.assignmentid = ata.assignmentid INNER JOIN ActivityTasks t ON ata.taskid = t.taskid ..."`.
- [x] Issue: removing a project causes the following error: "Table 'trial.MilestonesSteps' doesn't exist"
- [x] Issue: `ls -a` for projects should also display: projectmanagerid , projectno.
- [x] Issue: `ls -a` for activities should also display: activitydescription, plannedstart, plannedfinish, forecaststart, forecastfinish, actualstart, actualfinish, plannedhours, forecasthours.
- [x] Change the `Activities` folder to `Activity Categories` for consistency.
- [x] Improve the error handling code in the data classes in ./src/data/. Instead of throwing const char* exections, create and use exceptions classes that inherits the std exceptions.
- [x] Issue: Validation error is not handled anywhere in the stack. Program terminates by throwing the const char* .
- [x] When adding manually the user should be able to cancel by pressing `esc` at any time.
- [x] Need to add tables for Departments and ProjectTeams in the database and incorporate them in the heirarchy in CLAUDE.md.
- [x] Add a description and departmentId fields to ActivityTasks table.
- [x] Pending tables to implement in the heirarchy in CLAUDE.md: ActivityTaskAssignments, Hours, ArtefactToActivityLink, ArtefactData, MilestoneToArtefactLink 
- [x] Create a plan to implement the updated schema and heirarchy described in the CLAUDE.md file in the program. A brief description of the changes made to the schema are: 1. A new table `Departments` is created and the `Employees` table is linked to the new table through the `departmentid` foreign key. 2. Two new fields, `passwordsalt` and `role` are added to the `Employees` table. The `passwordsalt` will be used for hashing the password, and the `admin` field will indicate if the user has adminstration rights to modify data in the database. 3. A new table `ProjectTeamMembers` is created to associate employees to projects. The `role` field in this table describes the role the employee plays in the project: `0` value is a regular team member, `1` is a team lead, `2` is a project manager. 4. A new field `taskdescription` is added to the `ActivityTasks` table. A brief description of the changes made to the heirarchy are: 1. A new folder called `Organization` is created under which the `Departments` and `Employee` details are accessed. 2. A `Team` folder is created under `Projects`. 3. `Tasks` and `SubTasks` have two sub-folders: the first one is `Tasks` that lists all the sub-tasks, and the second is `Assignments` that lists allthe task assignments and the hours booked for that task. 4. Under `Artefacts` we have two folders: the first is `Data` that shows the artefact data, and the second is associations that lets the user access the `ArtefactsToActivityLinks` and `MilestoneToArtefactLinks` tables. 5. `Linkages` folder under `Projects` have now been removed. 
- [x] Issue: When there is a parameter to the `add` command it displays the error "Incomplete record in CSV data on line #2". This error should be "Unexpected data encountered. Use the following format to add an item: <csv format>". Use this for all the items where add is implemented. **Proposed solution:** The error occurs in data class CSV constructors (e.g., `src/data/projectdata.cpp:58-59`) when the CSV data has fewer fields than expected. Modify `CsvParseError` handling in `src/commands/add.cpp` to catch these exceptions and provide context-specific error messages. In the `add()` function's catch block (lines 233-237), add handling for `CsvParseError` separately from `DataError`: when caught, print a message like `"Error: Invalid format. Use: ad <field1>,<field2>,... (e.g., 'ad ProjectName,PRJ001,1' for projects)"`. Each path type case should define the expected format string. Alternatively, modify each data class CSV constructor to throw a custom `FormatError` exception that includes the expected format string.
- [x] Issue: When adding an activity planned dates and hour should be able to be set to null instead of getting the following database error: "Error creating activity: Incorrect date value: '' for column `trial`.`Activities`.`plannedstart` at row 1" **Proposed solution:** In `src/commands/add.cpp:addActivity()` (lines 451-460), the INSERT statement uses `setString()` for date fields even when they are empty strings. Modify the insert logic to check if the date/hour values are empty before setting: ```cpp if (act.getPlannedStart().empty()) { insertStmt->setNull(4, sql::DataType::DATE); } else { insertStmt->setString(4, act.getPlannedStart()); } ``` Apply the same pattern for `plannedfinish` (field 5) and `plannedhours` (field 6, using `sql::DataType::DECIMAL`). Additionally, update `src/data/activitydata.cpp:validate()` to allow empty strings for these optional fields by removing or relaxing the validation checks for dates and hours.
- [x] Issue: Adding a sub task should be possible only within the subtask folder, not under tasks **Proposed solution:** In `src/commands/add.cpp:83-101`, the code currently allows adding sub-tasks when `pth.current_type() == Path::TASK`. According to the hierarchy, adding a sub-task should only be allowed when inside the TASKS_FOLDER under a TASK (not directly on the TASK itself). Modify the logic: 1) Remove or modify the `Path::TASK` case (lines 83-101) to display an error message: `"Error: Cannot add an item here. Navigate to Tasks folder first."` 2) Update the `Path::TASKS_FOLDER` case (lines 73-82) to check if the parent is a TASK (indicating we're in a sub-tasks folder) by examining `pth.getIdByType(Path::TASK)`. If a parent task exists, create a sub-task with that as the parent; otherwise, create a top-level task with `parentTaskId = 0`. This ensures users must explicitly navigate to the Tasks sub-folder before adding sub-tasks.
- [x] Issue: Instead of "Enter department ID (0 for none): 0" when adding a task. just use  "(optional)" and handle a blank entry. **Proposed solution:** This issue refers to adding an employee (which prompts for department ID), not a task. In `src/data/employeedata.cpp:15` (and the lookup-enabled version around line 17-35), change the prompt from `"Enter department ID (0 for none): "` or `"Enter department ID/Name (0 for none): "` to `"Enter department ID/Name (optional): "`. Then modify the input handling to treat an empty string the same as "0": ```cpp std::string input = utils::promptString("Enter department ID/Name (optional): "); if (input.empty() || input == "0") { temp.departmentId = 0; } else { // existing lookup/parsing logic } ``` This provides a cleaner UX where users can simply press Enter to skip the optional field. 
- [x] Issue: The user should not be able to navigate to the last item in an heirarchy **Proposed solution:** Leaf nodes in the hierarchy (items with no children) should not be selectable. In `src/commands/select.cpp`, add guards in each `select*()` function to prevent navigation to leaf items. The leaf types are: `EMPLOYEE`, `PROJECT_TEAM_MEMBER`, `ACTIVITY_NOTE`, `HOURS`, `ARTEFACT_DATA_ITEM`, `MILESTONE_TO_ARTEFACT_LINK`, `ARTEFACT_FIELD`, and `MILESTONE_STEP`. Create a helper function `bool isLeafType(Path::ItemType type)` that returns true for these types. In the `select()` function (around line 148), after determining the target type, check if it's a leaf: if so, display `"Error: Cannot navigate further. This is the last item in the hierarchy."` and return without modifying the path. Alternatively, modify the individual select functions (e.g., `selectEmployee()`, `selectNote()`) to print the item details instead of navigating to it, providing useful feedback while preventing further navigation.
- [x] Issue: When adding a artefact data item the program should prompt the field names instead of "Enter artefact data field ID:" **Proposed solution:** In `src/data/artefactdataitemdata.cpp:8-17`, the constructor prompts for `artefactDataFieldId` directly. Since the lookup module (`src/lookup.cpp`) now supports `lookupArtefactDataField()` which is scoped by artefact type ID, this is partially addressed. However, to improve UX further: 1) Before prompting, query and display available fields: ```cpp auto stmt = db->prepareStatement("SELECT artefactdatafieldid, fieldtitle FROM ArtefactDataFields WHERE artefacttypeid = ?"); stmt->setInt(1, artefactTypeId); auto res = stmt->executeQuery(); std::cout << "Available fields:" << std::endl; while (res->next()) { std::cout << "  (" << res->getInt("artefactdatafieldid") << ") " << res->getString("fieldtitle") << std::endl; } ``` 2) Then prompt with `"Enter field ID/Name: "` using the existing `lookup::promptIdOrNameScoped()`. This shows users what fields are available before they enter a value, making the interface more intuitive.
- [x] Issue: Project manager need not be specified in `Projects` as it is defined in the `ProjectTeamMembers` table. **Proposed solution:** 1) In `database/schema/schema.sql`, remove the `projectmanagerid` column and its foreign key constraint from the `Projects` table. 2) In `src/data/projectdata.h`, remove the `managerId` field from the `Record` struct and the `getManagerId()` getter. 3) In `src/data/projectdata.cpp`, remove the prompt for manager ID in the default constructor (line 12), remove the manager ID parsing from the CSV constructor (lines 47-52), update the field count check from `< 3` to `< 2`, and remove manager ID from the database constructor query and result handling. 4) In `src/commands/add.cpp:addProject()`, remove the employee validation for project manager (lines 247-255) and remove `projectmanagerid` from the INSERT statement. 5) Update `src/commands/list.cpp` and any export functions to exclude the manager ID field. The project manager role is already captured in `ProjectTeamMembers` where `role = 2` indicates a project manager.
- [x] When adding an item manually for fields where it is required to provide an id, for example when entering the employee id when add a team member, the user should also be able to enter the name and the progam will lookup the id before adding. For example "Enter employee ID/Name: Rebecca Henderson" should be a valid entry. 
- [x] Add a login functionality that requires the user to enter a password to enter the program. The username will be the employees email id, passed to the program using the `-u` flag. Add a hashing functionality to store the hash in the database when the user sets the password for an employee. Use the salt field of the Employees table to store the salt.Add role based access functionality where only the admin user has rights to modify enteries in the database, all other users have only read-only access. 
- [x] During import validate the first row to check if the field names are as expected. 
- [x] During import when required to provide an ID, the user should be able to provide either the ID or NAME. The program should be able to lookup the ID if given NAMEs. The header line should reflect the choice of which of the two the user is providing.
- [x] Go through the file ./cerr.txt, replace all the occurences of std::cerr in the program with exceptions.
- [x] Create a comprehensive plan to update all the data classed to validate all input by the user to ensure data integrity. Validation logic should go to the validate function in the class.
- [x] Validation: check if project name and project id and unique.
- [x] Validation: On adding duplicate category,subcategory,activity,tasks,artefact type,fields, artefacts,artefact data, we get message:"Error. 1 duplicate entry found. Aborting import.". Give a better message.
- [x] Validation: On adding a task and providing a blank department id we get the message: "Validation error: Invalid integer value:". Blank value should be a 0 by default.
- [x] Validation: When adding an assigment for a task, the assigned date cannot be left empty.
- [x] Validation: When assigning a task to a user. Check if the user is assigned the task multiple times in the same time period. If the end date is blank, the user cannot be reassigned the task after the assign date. Currently the program just outputs the unhelpful message:"Error. 1 duplicate entry found. Aborting import."
- [x] Validation: Check for reserved charaters in names: `,`
- [x] Validation: When adding a artefact data entry and no fields are defined show an error message "No data fields are defined for artefact type <artefactype name>", instead of "Available fields:".
- [x] Validation: When adding an item and employee name is given, check to see if that employee is part of the project team. 
- [x] Issue: Validation error is displaying twice when a duplicate note is created. **Fixed:** Updated all 19 data classes to throw ValidationError with specific messages instead of printing to std::cerr and returning false. Changed validate() signature from `bool validate()` to `void validate()` in all header and cpp files.
- [x] Make all the data classes in ./src/data/ under a common namespace `data`
- [x] Create a class data from which all other classes are inherited. This class should define the reset() and next() functions.
- [x] complete the implementation of  `TaskData(Database& db, std::vector<std::map<std::string,std::string>> rows, int activityId, int taskId=0)` in ./src/data/taskdata.h similar to ./src/data/employeedata.h
- [x] define and implement a constructor to create a class from a vector of maps in activitydata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in artefactdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in artefactdataitemdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in artefacttoactivitylinkdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in artefacttypedata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in assignmentdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in categorydata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in departmentdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in fielddata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in hoursdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in milestonedata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in milestonestepdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in milestonetoartefactlinkdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in notedata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in path.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in projectdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in projectteammemberdata.h. Use ./src/data/taskdata.h for reference.
- [x] define and implement a constructor to create a class from a vector of maps in subcategorydata.h. Use ./src/data/taskdata.h for reference.
- [x] split the `add` function in ./src/commands/add.h in to two overloaded functions one with and the other without the csvValues.
- [x] Implement the parseCSVString() function in ./src/utils/csv_parser.cpp to get a string in the format `title1:value1,title2:value2,...` and return the data in a vector of maps.
- [x] Update the `add(Database& db, Path& pth, const std::string& csvValues)` function in ./src/commands/add.h to use the data constructor that returns a vector of maps. The case of `Path::DEPARTMENT` is already done.  
- [x] Update the `add` functions in ./src/commands/add.cpp to use the updated constructors in the data classes in ./src/data/ .
- [x] Update the `set` function in ./src/commands/set.cpp to use the updated constructors in the data classes in ./src/data/ .
- [x] Update the `importcsv` function in ./src/commands/importcsv.cpp to use the updated constructors in the data classes in ./src/data/ .
- [x] When using the `add` command with a csv string, convert the titles to lower case before using.
- [x] When using the `add` command to add an entry manually, update the prompt to be the field name without spaces and the first letter of each word capitalized. For example instead of "Enter first name:" use "FirstName". This will ensure that the field names when entering manually, adding by csv string, and while importing are consistent.
- [x] Update the source files in ./examples/source/1/ to use the new pattern to add using csv strings.
- [x] Update the validation functions in the data classes ./src/data/ to check for dupicate entries.
- [x] Issue: The user should not be able to add in the Organization folder. Moved department-adding logic from ORGANIZATION_FOLDER to DEPARTMENTS_FOLDER.
- [x] Issue: We get an error when we try to delete a project with team members: "Database error during deleting project: Cannot delete or update a parent row: a foreign key constraint fails (`trial`.`ProjectTeamMembers`, CONSTRAINT `ProjectTeamMembers_ibfk_1` FOREIGN KEY (`projectid`) REFERENCES `Projects` (`projectid`))"
- [x] Validation: Ensure that names do not start with a number.
- [x] Validation: Make `:` a reserved character.
- [x] Issue: Unable to add a subtask. Once a sub task is added it is not visible in the heirarchy.
- [x] Issue: Unable to select an task assignment by user name.
- [x] Issue: Using the `list` command with `-a` flag in the task folder does not show all the details in the task table. 
- [x] Issue: When adding a task the user should be able to provide either a department id or use the department name.
- [x] Issue: Remove the redundant 'Error:' prefix in the some of the exception handling logic. Add 'Error:' only at the end when the error is displayed.
- [x] In ./examples/source/1/project.src, when milestone steps should be referred by milestonestepname instead of milestonestepid.
- [x] In ./examples/source/1/project.src, when creating associations for artefacts, activity shoud be referred to using the artefactname instead of activityid.
- [x] In ./examples/source/1/project.src, when populating artefact field data, use artefactdatafieldname instead of artefactdatafieldid
- [x] Issue: Update the lookup funtions for artefact to activity links to lookup activities in the current project only.
- [x] Issue: Unable to select a artefact to activity link by name. For example the command: " ~/Projects/Building Project/Artefact Types/Drawing/Artefacts/Foundation Plan - Sheet S-101/Associations> cd "Prepare structural drawings"" gives the error "Error: Invalid selection 'Prepare structural drawings'."
- [x] Issue: Adding a milestone step using csv argument ([osoem] ~/Projects/Refinery Project/Milestones/Progress> ad Approved,1.0) causes an error: "Invalid progress ratio in CSV data" **Proposed solution:** In `src/data/milestonestepdata.cpp:46-50`, the `std::stod()` call fails when parsing the progress ratio. This likely occurs because the input contains unexpected whitespace or locale-specific decimal separators. Debug by adding logging to see the actual field value being parsed. Potential fixes: 1) Trim whitespace from the field before parsing: `field.erase(0, field.find_first_not_of(" \t")); field.erase(field.find_last_not_of(" \t") + 1);` 2) The issue may also be in `src/commands/add.cpp:159` where the CSV is constructed. If the user enters `ad Approved,1.0`, check that `createCsvData()` correctly formats it as `"milestoneid,milestonestepname,progressratio\n<id>,Approved,1.0"`. 3) Consider using a more robust parsing approach that handles edge cases, or provide a clearer error message that shows the actual value that failed to parse: `throw CsvParseError("Invalid progress ratio '" + field + "' in CSV data");` 
- [x] Issue: Unable of add a milestone step by name. For example: " ~/Projects/Building Project/Artefact Types/Drawing/Artefacts/Foundation Plan - Sheet S-101/Associations/Prepare structural drawings>ad "milestonestepname:Discipline Check,completedbyname:Michael Chen,completiondate:2026-03-15"" gives the error "Error: Validation error: Milestone step ID must be a positive integer"
- [x] Read and update the ./README.txt file as per the latest functionality of the program.
- [x] Supress the confirmation message when deleting an item using a source file.
- [x] Disable the ability to add an item manually when run from a source file.
- [x] Update the table headers in ./src/commands/list.cpp when the `list` command is called with the `-a` flag, such that the column titles should match the titles used when adding using a csv string. See ./src/commands/add.cpp to understand this format. Capitalize the first letter of each word for readability. For example `departmentname` would become `DepartmentName`. (Except for the `ID` column which would remain the same)  
- [x] Go through ./src/commands/set.h and ensure consistency with the other commands such as `add` in ./src/commands/add.h. Update the `set` command to take two parameters. The first parameter should be the `ID` of the item to edit and the second parameter should be a string with the format "title:value, title:value..." similar to the csv string for the `add` command. The given values should be updated for the item with the given id.
- [x] Correct the heirarchy structure in ./src/commands/set.h.
- [x] Update the set command to accept as the first parameter either an id or a name, similar to the select command. The name should also have an auto-complete feature.
- [x] Implement a tab auto-complete functionality for when the user uses the add command to manually create an item and has to provide an ID/Name from the database . Search the project team for a suitable match.
- [x] Make `~` a reserved character.
- [x] Redundant 'Invalid format' in error messages "Error: Validation error: Invalid format: Invalid format: expected 'title:value' but got 'asdf'. Use: st <id> "fieldname:value, ...""
- [x] Validation: Check for duplicate department names.
- [x] Validation: Check for duplicate email ids and firstname lastname combos.
- [x] Validation: Check for duplicate project names.
- [x] Validation: Check for duplicate category names in a project.
- [x] Validation: Check for duplicate subcategory names in a category.
- [x] Validation: Check for duplicate activity names in a subcategory.
- [x] Issue: Unable to set the activitydescription,forcaststart, forecastfinish, forecasthours fields of an activity using the set command.
- [x] Validation: Check for duplicate task names within an activity.
- [x] Issue: unable to update the taskdescription for a task.
- [x] Issue: Update ./src/commands/set.cpp to enable the parent id to be updated. Currently the datai classes allow for this but the set.cpp does not. For example projectid should be editable for a activity category using `set <id> "projectid:<new project id>"
- [x] Issue: Update ./src/commands/list.cpp to display the ids for fields that are obtained from other tables with the names in parenthesis. For example, when displaying the manager details using `ls -a`, show the manager id with the name in brackets.
- [x] Issue: Unable to set the manager fields of an activity using the set command.
- [x] Issue: unable to update the department for a task.
- [x] Artefact table should display both the artefactownerid and the artefactownername in brackets. 
- [x] Artifact to activity associations should show the activityid with the activity name in brackets.
- [x] Artifact to milestonestrp associations should show the milestonestepid with the milestonestep name in brackets.
- [x] Issue: In ./src/commands/list.cpp, remove `ActualStart` column from table for activities.
- [x] Issue: In ./src/commands/list.cpp, remove email from team table. Role should be displayed as integer followed by the description in brackets, for example `2 (Manager)`.
- [x] Validation: In ./src/data/assignmentdata.cpp, check if a user is assigned a task multiple times in the same time period. If a task is assigned to the user and not closed the user cannot be assigned the task again. If a task is assigned on a date before another assignment, a close date before the latter assignment should also be specified.
- [x] Validation: In ./src/data/assignementdata.cpp, check to ensure an assigned date is not after the date the acitivity is actual finish.
- [x] Validation: In ./src/data/activitydata.cpp, validate actual end date is after the actual start
- [x] Validation: In ./src/data/activitydata.cpp, validate that there is no time booked for the activity after the actual end date
- [x] Issue: Auto-complete does not work for a sub task with the `select` and `set` command for task assignments. The task name of the parent task is instead being displayed incorrectly.
- [x] In ./src/commands/set.h can we replace the functionality of resolveItemId() function with the functions in ./src/utils/lookup.h
- [x] Issue: Unable to set artefact field data value using the Field title.
- [x] Add a header to the ls command to clarify the current location.
- [x] Issue: The `set` command is not working in team folder. We get the error: "Error: Invalid ID: '225'. Must be an integer". Review resolveItemId() in ./src/command/set.h to identify other missing items in the heirarchy.
- [x] Issue: The `set` is not working in milestone to artefact association folder.We get the error: "Error: Invalid ID: '225'. Must be an integer". Review resolveItemId() in ./src/command/set.h to identify other missing items in the heirarchy.
- [x] Issue: The `set` is not working for hours booked in tasks assignements.We get the error: "Error: Invalid ID: '225'. Must be an integer". Review resolveItemId() in ./src/command/set.h to identify other missing items in the heirarchy.
- [x] Complete the list of Activities in ./examples/Data_Center_Design/README.md similar to examples/Water_Treatment_Plant/README.md
- [x] Add a `-i` flag to import that causes it to ignore duplicates
- [x] Use case insensitive lookup for field names when importing from csv files.
- [x] When importing data for artefact to activity link from a csv file, the user should be able to optionally provide categoryname and subcategoryname in addition to the activivityname to obtain a unique activity. Import logic is in ./src/commands/importcsv.h and the lookup logic is in ./src/utils/lookup.h
- [x] When importing or exporting from a source file all file location should be relative to the source file.
- [x] Issue: Core dump message is displayed when importing with the wrong file.
- [x] Check the code in addCategoriesRecursive() of ./src/commands/importcsv.cpp for bugs.
- [x] Implement the function addProjectsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addSubcategoriesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addActivitiesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addTasksRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addArtefactTypesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addArtefactsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addNotesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addFieldsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addMilestonesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addMilestoneStepsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addTeamMembersRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addAssignmentsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addHoursRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addArtefactDataItemsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addArtefactToActivityLinksRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addMilestoneToArtefactLinksRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addDepartmentsRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the function addEmployeesRecursive() in ./src/commands/importcsv.cpp similar to the function addCategoriesRecursive().
- [x] Implement the recursive functionality, using the `-r` flag, for the import command to recursively import data from a large csv table. Sample table to import is in ./examples/Data_Center_Design/activitydata.csv. Use the recursive add functions in ./src/commands/import.cpp.
- [x] In the data classes in ./src/data/*.h, for the constructor function that uses a vector of maps as argument, add logic to ignore duplicate values if `ignoreDuplicates` flag is passed. See sample in the file ./src/data/milestonedata.cpp, lines 18-30.
- [x] In the validation function, in all the data classes in ./src/data/*.h, while checking for duplicates, update the SQL to ensure we do not check against a record with the same id. See sample in the file ./src/data/milestonedata.cpp, line 100.
- [x] Remove the option to edit the parent id using the `set` command. For example for an artefact type the setValue function should not allow the projectid to be edited. 
- [x] In the data classes in ./src/data/*.cpp, add the ability to set at data item using a name in addition to id in the set value function. For example the user should be able to use either userid of username when using set for task assignments. 
- [x] Issue: In the setValue function of ./src/data/artefactdataitemdata.h, artefactdatafieldid should not be editable for a field.
- [x] Validation: When adding activity owner or artefact owner check if that user is part of the team.
- [x] Validation: When editing the user id in team table, check if the user is associated with any activity or artefact in the project. If so throw an error.
- [x] Test the recursive import commands for Activities.
- [x] Test the recursive import commands for Artefacts and Links.
- [x] Test the recursive import commands for Milestones.
- [x] In the addTasksRecursive() function in ./src/commands/importcsv.cpp add the logic to check if the parenttaskname or id is provided in the csv data, if so get the parent task id using the combination of taskname, actvityname, subcategoryname and categoryname.- [ ] Test the recursive import commands for tasks and assignments.
- [x] Test the recursive import commands for Tasks and Assignments.
- [x] Test the recursive import commands for Notes.
- [x] Test the recursive import commands for fields and data.
- [x] Test the recursive import commands for project teams.
- [x] Test the recursive import commands for departments and employees.
- [x] For the import command ,when providing the file to import, the user should be able to navigate the file structure in a manner similar to a terminal with a autocomplete feature when the user presses the tab.
- [x] The user should have the ablitilty to move through muliple levels of the heirarchy using the `cd ~/.../.../...` format similar to the terminal cs command. Make use of special charaters like `.` and `..` to represent the current and previous locations in the heirarchy. Make `/` a reserved character and add validations to ensure that no data begins with a `.`.
- [x] Issue: Export command - Unable to export data from Task Assignments and Hours Bookings locations.file: ./src/commands/exportscv.h - **Solution:** In `exportcsv.cpp`, add three missing branches to the `if-else` chain: 1. `ASSIGNMENTS_FOLDER`: `pth.current_id()` holds the task ID (set by `select.cpp` line 130). Query `SELECT userid, assigneddate, closedate FROM ActivityTaskAssignments WHERE taskid = ? ORDER BY assignmentid` with `pth.current_id()` as the parameter.  2. `ASSIGNMENT`: `pth.current_id()` holds the assignment ID. Query `SELECT bookeddate, hours FROM Hours WHERE assignmentid = ? ORDER BY bookingid`.  3. `HOURS`: Leaf node — throw `NavigationError("Cannot export from an individual hours booking. Navigate to the Assignments folder.")`.
- [x] Issue: Export command - Unable to export data from ArtefactFieldData, Activity and Milestone Associations locations.file: ./src/commands/exportcsv.h - **Solution:** In `exportcsv.cpp`, add missing branches: 1. `DATA_FOLDER`: `pth.current_id()` holds the artefact ID (set by `select.cpp` line 151). Query `SELECT adf.fieldtitle, ad.value FROM ArtefactData ad JOIN ArtefactDataFields adf ON ad.artefactdatafieldid = adf.artefactdatafieldid WHERE ad.artefactid = ? ORDER BY ad.artefactdataid`. Export header `fieldtitle,value`.  2. `ASSOCIATIONS_FOLDER`: `pth.current_id()` holds the artefact ID (set by `select.cpp` line 154). Query `SELECT a.activityname, aal.ratio FROM ArtefactToActivityLink aal JOIN Activities a ON aal.activityid = a.activityid WHERE aal.artefactid = ? ORDER BY aal.artefactlinkid`. Export header `activityname,ratio`.  3. `ARTEFACT_TO_ACTIVITY_LINK`: `pth.current_id()` holds the artefactlinkid. Query `SELECT ms.milestonestepname, mal.completedbyid, mal.completiondate FROM MilestoneToArtefactLink mal JOIN MilestoneSteps ms ON mal.milestonestepid = ms.milestonestepid WHERE mal.artefactlinkid = ? ORDER BY mal.milestonelinkid`. Export header `milestonestepname,completedbyid,completiondate`.  4. `MILESTONE_TO_ARTEFACT_LINK` and `ARTEFACT_DATA_ITEM`: Leaf nodes — throw `NavigationError`.
- [x] Issue: Export command - Unable to export from teams location. File: ./src/commands/exportcsv.h - **Solution:** In `exportcsv.cpp`, add two missing branches: 1. `TEAM_FOLDER`: `pth.current_id()` holds the project ID (set by `select.cpp` line 105). Query `SELECT e.firstname, e.lastname, e.email, ptm.role FROM ProjectTeamMembers ptm JOIN Employees e ON ptm.employeeid = e.employeeid WHERE ptm.projectid = ? ORDER BY ptm.teammemberid`. Export header `firstname,lastname,email,role`.  2. `PROJECT_TEAM_MEMBER`: Leaf node — throw `NavigationError("Cannot export from an individual team member.")`.
- [x] Issue: Export command - Unable to export from the `Departments` location. File: ./src/commands/exportcsv.h - **Solution:** In `exportcsv.cpp`, add two missing branches: 1. `DEPARTMENTS_FOLDER`: `pth.current_id()` returns the folder index (1), not a real DB ID, so do not use it as a filter. Query `SELECT departmentname FROM Departments ORDER BY departmentid` with no WHERE clause. Export header `departmentname`.  2. `ORGANIZATION_FOLDER`: Throw `NavigationError("Cannot export here. Navigate to Departments.")`.
- [x] Issue: Export command - Exporting employees exports all the employees not the ones in the department. File: ./src/commands/exportcsv.h - **Solution:** In `exportcsv.cpp`, the `DEPARTMENT` branch (line 45) uses a query without a department filter. Change it to a prepared statement: `SELECT firstname, lastname, email FROM Employees WHERE departmentid = ? ORDER BY employeeid`, binding `pth.current_id()` as the parameter. Replace the raw `db.executeQuery(query)` call with `db.prepareStatement(query)` / `stmt->setInt(1, pth.current_id())` / `stmt->executeQuery()` — consistent with how all other filtered branches work.
- [x] Implement a help functionality. Add a `help` command that displays the program details and usage instructions. For more details the user should be able to use `help <command>` to get more details on a command like syntax etc. (example: `help add` should give details on the add command).
- [x] Update the test plan and automated test files in ./test/ to incorporate the latest updates to the program. 
